version: 0.2

env:
  variables:
    ENV_NAME: dev

phases:
  build:
    commands:
      - psql --host=$DBEndPoint --dbname=$DBname --port=$Port --username=$DBUser --password=$Password < "moxa-devices.sql"
      - echo Deploy started on `date`
      - echo Deploying to $ENV_NAME environment...
artifacts:
   files:
     - '**/*'